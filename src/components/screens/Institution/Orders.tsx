import React from 'react';
import { View, Text } from 'react-native';

const InstitutionOrders: React.FC = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-2xl font-bold">Pedidos da Instituição</Text>
    </View>
  );
};

export default InstitutionOrders;
